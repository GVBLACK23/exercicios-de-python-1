# Crie uma função que receba uma lista de objetos (onde cada objeto tem um atributo 'nome') e retorne uma nova lista contendo apenas os nomes de cada objeto.

class Objeto:
    def __init__(self, nome):
        self.nome = nome

def extraindonomesdeobjetos(objetos):
    pass  # implemente a solução aqui

# Exemplo de uso
objetos = [Objeto("obj1"), Objeto("obj2"), Objeto("obj3")]
print(extraindonomesdeobjetos(objetos))
